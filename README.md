# StudentManagementSystem
Developed a Student Management System using Spring Boot with MVC architecture. The application supports CRUD operations for managing student records. Spring Data JPA and Hibernate handle database operations, while Thymeleaf and Bootstrap provide a responsive user interface.
